package com.clevertap.model;

public enum Operator {
  equals,
  not_equals,
  contains,
  does_not_contain,
  exists,
  does_not_exist,
  greater_than,
  greater_than_equals,
  less_than,
  less_than_equals
}
